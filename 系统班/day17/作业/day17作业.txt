

每日反馈,哇咔咔~

1 .现有一文件student.txt,内容如下：
张三
20
李四
18
王五
19

1.将文件中的数据通过合适的流读出，打印到控制台
2.编写Student类，具有name和age两个属性, 将从文件中读取到的姓名和年龄数据存储到Student对象中
3.将Student对象存储到集合中，迭代器遍历集合,打印出所有学生的姓名和年龄信息






2. 已知在“D:\\文件.txt”文件【文件编码为utf-8】中，保存着一些文本信息（直接准备好文件即可)，
请使用转换流编写程序将文本内容读取出来，将读取出的文件内容保存为字符串格式，并将字符串打印到控制台。

文件内容如下：

字符流的由来：因为数据编码的不同，而有了对字符进行高效操作的流对象。本质其实就是基于字节流读取时，去查了指定的码表。字节流和字符流的区别：
（1）读写单位不同：字节流以字节（8bit）为单位，字符流以字符为单位，根据码表映射字符，一次可能读多个字节。
（2）处理对象不同：字节流能处理所有类型的数据（如图片、avi等），而字符流只能处理字符类型的数据。
（3）字节流在操作的时候本身是不会用到缓冲区的，是文件本身的直接操作的；而字符流在操作的时候下后是会用到缓冲区的，是通过缓冲区来操作文件。





3. 扩展题目 : 大家试着使用匿名内部类的方式实现出一个多线程, 并开启线程执行线程中的run方法(明天课上能用上)